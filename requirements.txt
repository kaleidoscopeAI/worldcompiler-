numpy>=1.24
pytest>=7.0
fastapi>=0.115,<1.0
httpx>=0.27
opencv-python-headless>=4.10
python-multipart>=0.0.9
uvicorn>=0.30
# Optional: pretrained semantic channel (all-MiniLM-L6-v2, ~22 MB download)
# Uncomment or install separately to enable contextual world knowledge:
# sentence-transformers>=2.2
